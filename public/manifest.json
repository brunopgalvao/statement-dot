{
  "name": "statement.dot",
  "shortName": "statement",
  "version": "0.1.0",
  "description": "A humans-only social network. Statements you can verify, from humans you can trust.",
  "entry": "index.html",
  "icon": "icon.svg",
  "categories": ["social"],
  "permissions": [
    "ChainSubmit"
  ],
  "_notes": {
    "ChainSubmit": "Required — the app submits extrinsics (tips, contract calls) on Asset Hub.",
    "fonts": "Fonts are self-hosted (src/styles/fonts/), so no ExternalRequest permission is needed.",
    "taxonomy": "The Desktop permission taxonomy is still being finalized in the docs; adjust these to match the active Host build at publish time."
  }
}
